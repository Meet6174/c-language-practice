#include<stdio.h>
#include<string.h>
int main()
{
char st;
gets(st);//with gets you can enter the space
printf("%s",st);
puts(st);//after using the puts cursor will go in new line thats diffrent between puts and printf
int n=strlrn(st);
printf("the lenth of string is %d",n);
    return 0;
}